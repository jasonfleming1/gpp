<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
  <link rel="icon" type="image/svg+xml" href="/favicon.svg">
  <link rel="stylesheet" href="/css/style.css">
  <script src="https://unpkg.com/lenis@1.1.13/dist/lenis.min.js"></script>
  <script>
    (function() {
      const saved = localStorage.getItem('theme');
      if (saved) {
        document.documentElement.setAttribute('data-theme', saved);
      } else if (window.matchMedia('(prefers-color-scheme: dark)').matches) {
        document.documentElement.setAttribute('data-theme', 'dark');
      }
    })();
  </script>
  <style>
    /* Meeting Modal - Matches ASD Task Modal Design */
    .modal-meeting .modal-content {
      max-width: 800px;
      width: 95%;
      background: rgba(255, 255, 255, 0.95);
      backdrop-filter: blur(20px);
      -webkit-backdrop-filter: blur(20px);
      border: 1px solid rgba(255, 255, 255, 0.3);
      box-shadow:
        0 25px 50px rgba(0, 0, 0, 0.15),
        0 0 50px rgba(180, 37, 45, 0.1),
        inset 0 1px 0 rgba(255, 255, 255, 0.5);
    }

    [data-theme="dark"] .modal-meeting .modal-content {
      background: rgba(30, 41, 59, 0.95);
      border: 1px solid rgba(255, 255, 255, 0.1);
      box-shadow:
        0 25px 50px rgba(0, 0, 0, 0.4),
        0 0 60px rgba(214, 69, 80, 0.15),
        inset 0 1px 0 rgba(255, 255, 255, 0.05);
    }

    .modal-meeting .modal-header {
      background: linear-gradient(135deg, var(--brand-primary) 0%, var(--brand-primary-dark) 100%);
      color: white;
      border-radius: var(--radius-xl) var(--radius-xl) 0 0;
      padding: var(--space-4) var(--space-6);
    }

    .modal-meeting .modal-header h2 {
      color: white;
      margin: 0;
      font-size: 1.25rem;
    }

    .modal-meeting .modal-close {
      color: rgba(255, 255, 255, 0.8);
    }

    .modal-meeting .modal-close:hover {
      color: white;
      background: rgba(255, 255, 255, 0.15);
    }

    /* Two-column grid layout */
    .task-modal-grid {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: var(--space-6);
    }

    .task-modal-left {
      display: flex;
      flex-direction: column;
      gap: var(--space-3);
    }

    .form-row {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: var(--space-4);
    }

    .form-row .form-group {
      margin-bottom: 0;
    }

    /* Right panel with shaded background */
    .task-modal-right {
      display: flex;
      flex-direction: column;
      background: var(--surface-secondary);
      border-radius: var(--radius-lg);
      padding: var(--space-4);
      border: 1px solid var(--border-default);
    }

    [data-theme="dark"] .task-modal-right {
      background: rgba(0, 0, 0, 0.2);
      border-color: rgba(255, 255, 255, 0.1);
    }

    .task-modal-right .form-group {
      flex: 1;
      display: flex;
      flex-direction: column;
    }

    .task-modal-right textarea {
      flex: 1;
      min-height: 150px;
      resize: vertical;
    }

    @media (max-width: 700px) {
      .task-modal-grid {
        grid-template-columns: 1fr;
      }
      .task-modal-right {
        max-height: 250px;
      }
      .modal-meeting .modal-content {
        max-width: 95%;
      }
    }

    /* Date Input Theme Support */
    input[type="date"] {
      color-scheme: light;
    }

    [data-theme="dark"] input[type="date"] {
      color-scheme: dark;
    }

    input[type="date"]::-webkit-calendar-picker-indicator {
      filter: none;
      opacity: 0.7;
      cursor: pointer;
    }

    [data-theme="dark"] input[type="date"]::-webkit-calendar-picker-indicator {
      filter: invert(1);
    }

    input[type="date"]::-webkit-calendar-picker-indicator:hover {
      opacity: 1;
    }

    /* Form inputs glow on focus */
    .modal-meeting input:focus,
    .modal-meeting select:focus,
    .modal-meeting textarea:focus {
      box-shadow:
        0 0 0 3px rgba(180, 37, 45, 0.15),
        0 0 20px rgba(180, 37, 45, 0.1);
    }

    [data-theme="dark"] .modal-meeting input:focus,
    [data-theme="dark"] .modal-meeting select:focus,
    [data-theme="dark"] .modal-meeting textarea:focus {
      box-shadow:
        0 0 0 3px rgba(214, 69, 80, 0.2),
        0 0 25px rgba(214, 69, 80, 0.15);
    }
  </style>
</head>
<body>
  <nav class="navbar">
    <div class="navbar-brand">
      <a href="/">ASD Tracker</a>
    </div>
    <div class="navbar-menu">
      <a href="/" class="nav-link">Dashboard</a>
      <div class="nav-dropdown">
        <button class="nav-dropdown-toggle">
          Team
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <polyline points="6 9 12 15 18 9"></polyline>
          </svg>
        </button>
        <div class="nav-dropdown-menu">
          <a href="/tasks">Tasks</a>
          <a href="/scorecard">Scorecard</a>
        </div>
      </div>
      <a href="/meetings" class="nav-link">Meetings</a>
      <a href="/managertasks" class="nav-link">Manager Tasks</a>
      <a href="/releases" class="nav-link">Releases</a>
      <button id="themeToggle" class="theme-toggle" title="Toggle theme"></button>
    </div>
  </nav>

  <!-- Add Meeting Modal -->
  <div id="meetingModal" class="modal modal-meeting hidden">
    <div class="modal-content">
      <div class="modal-header">
        <h2 id="modalTitle">Add Meeting</h2>
        <button class="modal-close" onclick="closeModal()">&times;</button>
      </div>
      <div class="modal-body">
        <form id="meetingForm">
          <input type="hidden" id="meetingId">
          <div class="task-modal-grid">
            <!-- Left Column: Meeting Details -->
            <div class="task-modal-left">
              <div class="form-row">
                <div class="form-group">
                  <label for="meetingDate">Date</label>
                  <input type="date" id="meetingDate" required>
                </div>
                <div class="form-group">
                  <label for="meetingDuration">Duration (hrs)</label>
                  <input type="number" id="meetingDuration" step="0.25" min="0" required placeholder="e.g., 0.5">
                </div>
              </div>
              <div class="form-group">
                <label for="meetingType">Meeting Type</label>
                <select id="meetingType" required>
                  <option value="">Select type...</option>
                </select>
              </div>
              <div class="form-group">
                <label for="meetingEmployee">Employee/Team</label>
                <select id="meetingEmployee" required>
                  <option value="">Select employee...</option>
                </select>
              </div>
            </div>
            <!-- Right Column: Notes -->
            <div class="task-modal-right">
              <div class="form-group">
                <label for="meetingSummary">Summary / Notes</label>
                <textarea id="meetingSummary" placeholder="Key discussion points, action items, decisions made..."></textarea>
              </div>
            </div>
          </div>
        </form>
      </div>
      <div class="modal-footer">
        <button class="btn btn-secondary" onclick="closeModal()">Cancel</button>
        <button class="btn btn-primary" onclick="saveMeeting()">Save Meeting</button>
      </div>
    </div>
  </div>

  <main class="container">
    <div class="dashboard">
      <div class="page-header">
        <h1>Meetings</h1>
        <button class="btn btn-primary" onclick="openAddModal()">Add Meeting</button>
      </div>

      <!-- Loading State -->
      <div id="loadingState">
        <div class="skeleton-table">
          <div class="skeleton-row" style="background: var(--surface-tertiary);">
            <div class="skeleton-cell medium" style="background: transparent;"></div>
            <div class="skeleton-cell medium" style="background: transparent;"></div>
            <div class="skeleton-cell medium" style="background: transparent;"></div>
            <div class="skeleton-cell small" style="background: transparent;"></div>
            <div class="skeleton-cell title" style="background: transparent;"></div>
            <div class="skeleton-cell actions" style="background: transparent;"></div>
          </div>
          <div class="skeleton-row"><div class="skeleton-cell medium"></div><div class="skeleton-cell medium"></div><div class="skeleton-cell medium"></div><div class="skeleton-cell small"></div><div class="skeleton-cell title"></div><div class="skeleton-cell actions"></div></div>
          <div class="skeleton-row"><div class="skeleton-cell medium"></div><div class="skeleton-cell medium"></div><div class="skeleton-cell medium"></div><div class="skeleton-cell small"></div><div class="skeleton-cell title"></div><div class="skeleton-cell actions"></div></div>
          <div class="skeleton-row"><div class="skeleton-cell medium"></div><div class="skeleton-cell medium"></div><div class="skeleton-cell medium"></div><div class="skeleton-cell small"></div><div class="skeleton-cell title"></div><div class="skeleton-cell actions"></div></div>
          <div class="skeleton-row"><div class="skeleton-cell medium"></div><div class="skeleton-cell medium"></div><div class="skeleton-cell medium"></div><div class="skeleton-cell small"></div><div class="skeleton-cell title"></div><div class="skeleton-cell actions"></div></div>
          <div class="skeleton-row"><div class="skeleton-cell medium"></div><div class="skeleton-cell medium"></div><div class="skeleton-cell medium"></div><div class="skeleton-cell small"></div><div class="skeleton-cell title"></div><div class="skeleton-cell actions"></div></div>
          <div class="skeleton-row"><div class="skeleton-cell medium"></div><div class="skeleton-cell medium"></div><div class="skeleton-cell medium"></div><div class="skeleton-cell small"></div><div class="skeleton-cell title"></div><div class="skeleton-cell actions"></div></div>
          <div class="skeleton-row"><div class="skeleton-cell medium"></div><div class="skeleton-cell medium"></div><div class="skeleton-cell medium"></div><div class="skeleton-cell small"></div><div class="skeleton-cell title"></div><div class="skeleton-cell actions"></div></div>
          <div class="skeleton-row"><div class="skeleton-cell medium"></div><div class="skeleton-cell medium"></div><div class="skeleton-cell medium"></div><div class="skeleton-cell small"></div><div class="skeleton-cell title"></div><div class="skeleton-cell actions"></div></div>
          <div class="skeleton-row"><div class="skeleton-cell medium"></div><div class="skeleton-cell medium"></div><div class="skeleton-cell medium"></div><div class="skeleton-cell small"></div><div class="skeleton-cell title"></div><div class="skeleton-cell actions"></div></div>
          <div class="skeleton-row"><div class="skeleton-cell medium"></div><div class="skeleton-cell medium"></div><div class="skeleton-cell medium"></div><div class="skeleton-cell small"></div><div class="skeleton-cell title"></div><div class="skeleton-cell actions"></div></div>
        </div>
      </div>

      <!-- Main Content -->
      <div id="mainContent" style="display: none;">
        <!-- Filters -->
        <div class="toolbar">
          <div class="filters" id="typeFilters">
            <button class="filter-btn active" data-filter="all">All</button>
          </div>
          <div class="search-bar">
            <input type="text" id="searchInput" placeholder="Search meetings...">
          </div>
        </div>

        <!-- Meetings Table -->
        <div class="recent-tasks">
          <table class="data-table">
            <thead>
              <tr>
                <th class="sortable" data-sort="date">Date</th>
                <th class="sortable" data-sort="type">Type</th>
                <th class="sortable" data-sort="employee">Employee</th>
                <th class="sortable" data-sort="meetingDuration">Duration</th>
                <th>Summary</th>
                <th>Actions</th>
              </tr>
            </thead>
            <tbody id="meetingsTable">
            </tbody>
          </table>
          <div class="pagination" id="pagination"></div>
        </div>
      </div>

      <!-- Empty State -->
      <div id="emptyState" class="empty-state" style="display: none;">
        <h3>No Meetings Yet</h3>
        <p>Start tracking your meetings by adding your first one.</p>
        <button class="btn btn-primary" onclick="openAddModal()">Add First Meeting</button>
      </div>
    </div>
  </main>

  <script src="/js/app.js"></script>
  <script>
    let currentPage = 1;
    let currentFilter = 'all';
    let currentSearch = '';
    let sortField = 'date';
    let sortOrder = 'desc';

    // Initialize
    document.addEventListener('DOMContentLoaded', () => {
      loadFormOptions();
      loadMeetings();
      setupEventListeners();
    });

    async function loadFormOptions() {
      try {
        const [typesRes, employeesRes] = await Promise.all([
          fetch('/api/options/meetingTypes'),
          fetch('/api/options/meetingEmployees')
        ]);
        const typesData = await typesRes.json();
        const employeesData = await employeesRes.json();

        const typeSelect = document.getElementById('meetingType');
        typeSelect.innerHTML = '<option value="">Select type...</option>' +
          typesData.values.map(v => `<option value="${v}">${v}</option>`).join('');

        const empSelect = document.getElementById('meetingEmployee');
        empSelect.innerHTML = '<option value="">Select employee...</option>' +
          employeesData.values.map(v => `<option value="${v}">${v}</option>`).join('');

        // Populate filter buttons
        const filtersDiv = document.getElementById('typeFilters');
        filtersDiv.innerHTML = '<button class="filter-btn active" data-filter="all">All</button>' +
          typesData.values.map(v => `<button class="filter-btn" data-filter="${v}">${v}</button>`).join('');

        // Re-bind filter button events
        filtersDiv.querySelectorAll('.filter-btn').forEach(btn => {
          btn.addEventListener('click', () => {
            filtersDiv.querySelectorAll('.filter-btn').forEach(b => b.classList.remove('active'));
            btn.classList.add('active');
            currentFilter = btn.dataset.filter;
            currentPage = 1;
            loadMeetings();
          });
        });
      } catch (error) {
        console.error('Failed to load form options:', error);
      }
    }

    function setupEventListeners() {
      // Search
      let searchTimeout;
      document.getElementById('searchInput').addEventListener('input', (e) => {
        clearTimeout(searchTimeout);
        searchTimeout = setTimeout(() => {
          currentSearch = e.target.value;
          currentPage = 1;
          loadMeetings();
        }, 300);
      });

      // Sort headers
      document.querySelectorAll('.data-table th.sortable').forEach(th => {
        th.addEventListener('click', () => {
          const field = th.dataset.sort;
          if (sortField === field) {
            sortOrder = sortOrder === 'asc' ? 'desc' : 'asc';
          } else {
            sortField = field;
            sortOrder = 'desc';
          }
          document.querySelectorAll('.data-table th.sortable').forEach(h => h.classList.remove('active'));
          th.classList.add('active');
          loadMeetings();
        });
      });
    }

    async function loadMeetings() {
      const params = new URLSearchParams({
        page: currentPage,
        limit: 25,
        sortField,
        sortOrder
      });

      if (currentFilter !== 'all') params.set('type', currentFilter);
      if (currentSearch) params.set('search', currentSearch);

      try {
        const res = await fetch(`/api/meetings?${params}`);
        const data = await res.json();

        document.getElementById('loadingState').style.display = 'none';

        if (data.pagination.total === 0 && currentFilter === 'all' && !currentSearch) {
          document.getElementById('emptyState').style.display = 'block';
          document.getElementById('mainContent').style.display = 'none';
        } else {
          document.getElementById('emptyState').style.display = 'none';
          document.getElementById('mainContent').style.display = 'block';
          renderMeetingsTable(data.meetings);
          renderPagination(data.pagination);
        }
      } catch (error) {
        console.error('Failed to load meetings:', error);
        document.getElementById('loadingState').innerHTML = '<p class="error">Failed to load meetings</p>';
      }
    }

    // Format date for display (uses UTC to avoid timezone issues)
    function formatDateForDisplay(dateStr) {
      const d = new Date(dateStr);
      const months = ['Jan', 'Feb', 'Mar', 'Apr', 'May', 'Jun', 'Jul', 'Aug', 'Sep', 'Oct', 'Nov', 'Dec'];
      return `${months[d.getUTCMonth()]} ${d.getUTCDate()}, ${d.getUTCFullYear()}`;
    }

    function renderMeetingsTable(meetings) {
      const tbody = document.getElementById('meetingsTable');

      if (meetings.length === 0) {
        tbody.innerHTML = '<tr><td colspan="6" style="text-align:center;color:var(--text-muted);">No meetings found</td></tr>';
        return;
      }

      tbody.innerHTML = meetings.map(m => {
        const date = formatDateForDisplay(m.date);
        const summary = m.summary ? (m.summary.length > 50 ? m.summary.substring(0, 50) + '...' : m.summary) : '-';

        return `
          <tr>
            <td><strong>${date}</strong></td>
            <td><span class="badge info">${m.type}</span></td>
            <td>${m.employee}</td>
            <td>${m.meetingDuration} hrs</td>
            <td class="truncate">${summary}</td>
            <td>
              <button class="btn btn-sm btn-secondary" onclick="editMeeting('${m._id}')">Edit</button>
              <button class="btn btn-sm btn-danger" onclick="deleteMeeting('${m._id}')">Delete</button>
            </td>
          </tr>
        `;
      }).join('');
    }

    function renderPagination(pagination) {
      const container = document.getElementById('pagination');
      if (pagination.pages <= 1) {
        container.innerHTML = '';
        return;
      }

      let html = '';
      for (let i = 1; i <= pagination.pages; i++) {
        html += `<a href="#" class="page-link ${i === pagination.page ? 'active' : ''}" onclick="goToPage(${i}); return false;">${i}</a>`;
      }
      container.innerHTML = html;
    }

    function goToPage(page) {
      currentPage = page;
      loadMeetings();
    }

    // Format local date for input (for "today" - uses local timezone)
    function formatLocalDateForInput(date) {
      const d = new Date(date);
      const year = d.getFullYear();
      const month = String(d.getMonth() + 1).padStart(2, '0');
      const day = String(d.getDate()).padStart(2, '0');
      return `${year}-${month}-${day}`;
    }

    // Format UTC date for input (for dates from database - stored in UTC)
    function formatUTCDateForInput(date) {
      const d = new Date(date);
      const year = d.getUTCFullYear();
      const month = String(d.getUTCMonth() + 1).padStart(2, '0');
      const day = String(d.getUTCDate()).padStart(2, '0');
      return `${year}-${month}-${day}`;
    }

    // Modal functions
    function openAddModal() {
      document.getElementById('modalTitle').textContent = 'Add Meeting';
      document.getElementById('meetingForm').reset();
      document.getElementById('meetingId').value = '';
      document.getElementById('meetingDate').value = formatLocalDateForInput(new Date());
      document.getElementById('meetingModal').classList.remove('hidden');
    }

    async function editMeeting(id) {
      try {
        const res = await fetch(`/api/meetings/${id}`);
        const meeting = await res.json();

        document.getElementById('modalTitle').textContent = 'Edit Meeting';
        document.getElementById('meetingId').value = meeting._id;
        document.getElementById('meetingDate').value = formatUTCDateForInput(meeting.date);
        document.getElementById('meetingType').value = meeting.type;
        document.getElementById('meetingEmployee').value = meeting.employee;
        document.getElementById('meetingDuration').value = meeting.meetingDuration;
        document.getElementById('meetingSummary').value = meeting.summary || '';

        document.getElementById('meetingModal').classList.remove('hidden');
      } catch (error) {
        alertManager.showAlert('Failed to load meeting: ' + error.message, 'error');
      }
    }

    function closeModal() {
      document.getElementById('meetingModal').classList.add('hidden');
    }

    async function saveMeeting() {
      const id = document.getElementById('meetingId').value;
      const data = {
        date: document.getElementById('meetingDate').value,
        type: document.getElementById('meetingType').value,
        employee: document.getElementById('meetingEmployee').value,
        meetingDuration: parseFloat(document.getElementById('meetingDuration').value),
        summary: document.getElementById('meetingSummary').value
      };

      try {
        const url = id ? `/api/meetings/${id}` : '/api/meetings';
        const method = id ? 'PUT' : 'POST';

        const res = await fetch(url, {
          method,
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify(data)
        });

        if (!res.ok) {
          const error = await res.json();
          throw new Error(error.error);
        }

        closeModal();
        loadMeetings();
      } catch (error) {
        alertManager.showAlert('Failed to save meeting: ' + error.message, 'error');
      }
    }

    function deleteMeeting(id) {
      alertManager.showConfirm({
        title: 'Delete Meeting',
        message: 'Are you sure you want to delete this meeting?',
        confirmText: 'Delete',
        confirmClass: 'btn-danger',
        onConfirm: async () => {
          try {
            const res = await fetch(`/api/meetings/${id}`, { method: 'DELETE' });
            if (!res.ok) {
              const error = await res.json();
              throw new Error(error.error);
            }
            loadMeetings();
          } catch (error) {
            alertManager.showAlert('Failed to delete meeting: ' + error.message, 'error');
          }
        }
      });
    }

  </script>
</body>
</html>
